# Drupal CI Templates

[![CI](https://github.com/mykolapodpriatov/drupal-ci-templates/actions/workflows/meta.yml/badge.svg)](https://github.com/mykolapodpriatov/drupal-ci-templates/actions/workflows/meta.yml)

Copy-paste CI/CD pipeline templates for Drupal 10 and Drupal 11 projects. Covers
GitHub Actions, GitLab CI, and Bitbucket Pipelines, plus a small set of shared
shell helpers and tool configs (PHPStan, PHPCS, PHPUnit) that the pipelines
expect to find in your project root.

The goal is to give a maintainer the *thinnest possible* path from "fresh
Drupal repo" to "green pipeline running lint, static analysis, unit, kernel,
and functional tests with a deploy step on `main`" — without inventing a new
abstraction layer on top of the CI providers.

## What's included

```
github-actions/
  ci-full.yml            # PHP 8.2/8.3 × Drupal ^10.3/^11 matrix, all test stages
  ci-minimal.yml         # lint + phpstan + unit only — for small contrib modules
  ci-monorepo.yml        # changed-files filter for multi-module repos
  release.yml            # tag-triggered GitHub Release with autogenerated notes
  nightly.yml            # daily run against drupal/core 11.x-dev

gitlab-ci/
  .gitlab-ci.yml         # full pipeline mirroring ci-full.yml
  .gitlab-ci-minimal.yml # lighter variant
  templates/php-job.yml  # reusable job definitions for `include:`

bitbucket-pipelines/
  bitbucket-pipelines.yml

circleci/
  config.yml             # full pipeline mirroring ci-full.yml (copy to .circleci/config.yml)

scripts/
  setup-drupal.sh        # bootstraps a Drupal install for kernel/functional
  run-phpstan.sh         # phpstan with memory limit + neon resolution
  run-phpcs.sh           # phpcs against Drupal + DrupalPractice
  wait-for-mysql.sh      # poll mysqladmin until the service is ready

phpstan.neon.dist        # level 8 baseline with mglaman/phpstan-drupal
phpcs.xml.dist           # Drupal + DrupalPractice, sensible excludes
phpunit.xml.dist         # three testsuites: unit / kernel / functional

examples/recipes/        # how to drop the templates into different layouts
```

## Quick start

For a typical `drupal/recommended-project`-based repo:

1. Pick one workflow file, e.g. `github-actions/ci-full.yml`.
2. Copy it into your repo as `.github/workflows/ci.yml`.
3. Copy `phpstan.neon.dist`, `phpcs.xml.dist`, and `phpunit.xml.dist` to your
   repo root (rename to drop `.dist` if you want to commit them as-is, or keep
   `.dist` and add the non-`.dist` versions to `.gitignore` so devs can
   override locally).
4. Copy the relevant helpers from `scripts/` to `scripts/ci/` in your repo and
   commit them with the executable bit set (`git update-index --chmod=+x`).
5. Adjust the `working-directory` / path filters at the top of the workflow to
   match your layout (`web/` vs `docroot/`, custom modules path, etc.).
6. Push a branch and watch the matrix run.

For GitLab and Bitbucket the same idea applies — copy the file to the repo
root, adjust paths. For CircleCI, copy `circleci/config.yml` to
`.circleci/config.yml` and adjust paths the same way.

## Matrix overview

| Stage      | PHP 8.2 / Drupal ^10.3 | PHP 8.2 / Drupal ^11 | PHP 8.3 / Drupal ^10.3 | PHP 8.3 / Drupal ^11 |
| ---------- | :--------------------: | :------------------: | :--------------------: | :------------------: |
| Lint       | yes                    | n/a                  | yes                    | n/a                  |
| PHPStan    | yes                    | excluded             | yes                    | yes                  |
| Unit       | yes                    | excluded             | yes                    | yes                  |
| Kernel     | yes                    | excluded             | yes                    | yes                  |
| Functional | yes                    | excluded             | yes                    | yes                  |

Drupal 11 requires **PHP 8.3 or newer**, so the PHP 8.2 × `^11` combination can
never resolve. The matrix therefore excludes it (GitHub Actions uses an
`exclude:` block; GitLab enumerates the valid pairs; Bitbucket hard-codes the
pairing per step): PHP 8.2 is only paired with Drupal `^10.3`, and `^11` only
with PHP 8.3+. Lint runs once per PHP version (Drupal version irrelevant), so it
has no `^11` column. If you only target Drupal 11, drop the `^10.3` rows and the
PHP 8.2 jobs entirely.

## Deploy stage notes

The `deploy` job in `ci-full.yml` runs only on `push` to `main` and is left as
a *placeholder*. Four common patterns are documented in inline comments inside
the workflow:

- **rsync over SSH** — minimal, works for any host. Needs an SSH key in repo
  secrets and the target path on the server. Example included.
- **Pantheon Terminus** — `terminus auth:login --machine-token` then
  `terminus build:env:push` / `terminus env:deploy`. Auth token in
  `TERMINUS_TOKEN` secret.
- **Acquia BLT** — typically `blt artifact:deploy --commit-msg "..." --branch
  master-build`. Needs SSH access to Acquia git remote.
- **Generic Composer artifact** — `composer install --no-dev` into a separate
  `build/` dir, then rsync or git-push the artifact to a deploy remote. The
  template includes a small inline shell snippet you can uncomment.

Pick one, delete the other three, and commit your secret references through
the CI platform's UI — never inline.

## Requirements assumed by the templates

- `composer.json` at the repo root.
- `composer.lock` **committed** to the repo. The security job runs
  `composer audit --locked`, which reads the lock file; the shipped
  `.gitignore` therefore does *not* ignore it (site repos commit the lock).
- A `tests/` directory (or `web/modules/custom/*/tests/`) discoverable by
  `phpunit.xml.dist`.
- For kernel/functional: a MySQL/MariaDB service. The templates spin one up
  automatically; you don't need an external DB.
- For functional: a WebDriver endpoint for Mink — the templates run a
  `selenium/standalone-chrome` container as a service (reached over
  `MINK_DRIVER_ARGS_WEBDRIVER`); you don't run `chromedriver` yourself.

## Versioning policy

Templates follow [Semantic Versioning](https://semver.org/). The current line
targets Drupal 10.3+ and Drupal 11.x. When Drupal 12 lands, a `2.x` branch
will be cut and `1.x` will move to maintenance.

See [`CHANGELOG.md`](CHANGELOG.md) for the per-release diff.

## Contributing

Patches welcome — see [`CONTRIBUTING.md`](CONTRIBUTING.md). The meta-workflow
under `.github/workflows/meta.yml` lints this repo's own YAML on every PR, so
your changes will be checked by `yamllint` and `actionlint` before review.

## License

[MIT](LICENSE).
